// Set up a koa router
// Add in the router from the customer code
// Cache the result.
//
// Take the Fusebit ctx and create a Koa ctx from it.
// Choose a simple router and dispatch the ctx down that router.
// Respond back to the caller
//
// Lay down a set of handlers to do "connector" things <--- this is probably the only difference between the
// two packages.
//
//
// Add the capability to dispatch additional requests to the router for event triggering.
