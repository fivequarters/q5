{
  "name": "@5qtrs/cron",
  "version": "0.0.1",
  "description": "",
  "main": "libc/index.js",
  "sideEffects": false,
  "license": "UNLICENSED",
  "author": "FiveQuarters.io",
  "scripts": {
    "build": "tsc -b && yarn build:pack",
    "build:pack": "rm -f libc/cron.zip && zip -j libc/cron.zip libc/*.js && zip -qdgds 1m -r libc/cron.zip node_modules",
    "test": "jest --colors",
    "coverage": "jest --colors --coverage",
    "lint": "tslint --fix -t stylish src/{,**/}*.ts{,x} test/{,**/}*.ts{,x}",
    "clean": "rm -r -f libc; rm -r -f coverage"
  },
  "keywords": [],
  "dependencies": {
    "@5qtrs/runtime-common": "^0.0.1",
    "async": "^3.2.0",
    "cron-parser": "^2.7.3",
    "http-errors": "^1.7.1",
    "jsonwebtoken": "^8.5.0",
    "uuid": "^7.0.2"
  }
}
