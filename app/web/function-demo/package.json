{
  "name": "@5qtrs/function-demo",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "license": "UNLICENSED",
  "author": "FiveQuarters.io",
  "scripts": {
    "all": "yarn run -s lint && yarn run -s build && yarn run -s coverage",
    "build": "tsc -b",
    "test": "jest --colors",
    "coverage": "jest --colors --coverage",
    "lint": "tslint --fix -t stylish src/{,**/}*.ts{,x} test/{,**/}*.ts{,x}",
    "clean": "rm -r -f lib; rm -r -f coverage",
    "start": "cp ../../../lib/client/q5-editor/dist/* public/js/ && cp ../../../lib/client/q5-hook/dist/* public/js/ && tsc -b && nodemon ./devServer.js & tsc -b -W"
  },
  "keywords": [],
  "dependencies": {
    "@5qtrs/config": "^0.0.1",
    "@5qtrs/server": "^0.0.1",
    "cookie-parser": "^1.4.3",
    "dotenv": "^6.2.0",
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "http-errors": "^1.7.1",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "@5qtrs/request": "^0.0.1",
    "@types/cookie-parser": "^1.4.1",
    "@types/express": "^4.16.1",
    "@types/http-errors": "^1.6.1",
    "@types/morgan": "^1.7.35"
  },
  "devServer": {
    "port": 3000
  }
}
