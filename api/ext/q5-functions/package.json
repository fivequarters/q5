{
  "name": "@5qtrs/q5-functions",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "license": "UNLICENSED",
  "author": "FiveQuarters.io",
  "scripts": {
    "all": "yarn run -s lint && yarn run -s build && yarn run -s coverage",
    "build": "tsc -b",
    "test": "jest --colors",
    "coverage": "jest --colors --coverage",
    "lint": "tslint --fix -t stylish src/** test/**",
    "lint:src": "tslint --fix -t stylish src/**",
    "lint:test": "tslint --fix -t stylish test/**",
    "clean": "rm -r -f lib; rm -r -f coverage",
    "clean:build": "rm -r -f lib",
    "clean:coverage": "rm -r -f coverage",
    "start": "node lib/index.js"
  },
  "keywords": [],
  "dependencies": {
    "@5qtrs/q5-functions-lambda": "^0.0.1",
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "http-errors": "^1.7.1",
    "joi": "^14.3.1",
    "morgan": "^1.9.1",
    "swagger-ui-express": "^4.0.2",
    "yamljs": "^0.3.0",
    "zeromq": "^5.1.0"
  },
  "devDependencies": {},
  "devServer": {
    "port": 3001
  }
}
