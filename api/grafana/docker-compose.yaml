version: "3"
services:

  tempo:
    image: grafana/tempo:latest
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./tempo-config.yaml:/etc/tempo.yaml
      - ./tempo-data:/tmp/tempo
    ports:
      - "14268:14268/udp"  # jaeger ingest
      - "14268:14268/tcp"  # jaeger ingest
      - "55680:55680/udp"  # grpc ingest
      - "55680:55680/tcp"  # grpc ingest
      - "4317:4317/udp"  # grpc ingest
      - "4317:4317/tcp"  # grpc ingest
#    logging:
#      driver: loki
#      options:
#        loki-url: 'http://localhost:3100/loki/api/v1/push'

  loki:
    image: grafana/loki:2.3.0
    command: ["-config.file=/etc/loki/loki-config.yaml"]
    volumes:
      - ./loki-config.yaml:/etc/loki/loki-config.yaml
    ports:
      - "3100:3100"     # loki needs to be exposed so it receives logs
#    environment:
#      - JAEGER_AGENT_HOST=tempo
#      - JAEGER_ENDPOINT=http://tempo:14268/api/traces   # send traces to Tempo
#      - JAEGER_SAMPLER_TYPE=const
#      - JAEGER_SAMPLER_PARAM=1
#    logging:
#      driver: loki
#      options:
#        loki-url: 'http://localhost:3100/loki/api/v1/push'

#  prometheus:
#    image: prom/prometheus:latest
#    command: [ "--config.file=/etc/prometheus.yaml" ]
#    volumes:
#      - ./prometheus.yaml:/etc/prometheus.yaml
#    ports:
#      - "9090:9090"
#    logging:
#      driver: loki
#      options:
#        loki-url: 'http://localhost:3100/loki/api/v1/push'
#
  grafana:
    image: grafana/grafana:8.1.6
    command: ["--config=conf/grafana-config.ini"]
    volumes:
      - ./grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
      - ./grafana-config.ini:/usr/share/grafana/conf/grafana-config.ini
#    environment:
#      - GF_AUTH_ANONYMOUS_ENABLED=false
#      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
#      - GF_AUTH_DISABLE_LOGIN_FORM=true
    ports:
      - "3000:3000"
#    logging:
#      driver: loki
#      options:
#        loki-url: 'http://localhost:3100/loki/api/v1/push'
