{
  "name": "@5qtrs/lambda-nuke",
  "version": "0.0.1",
  "description": "",
  "main": "libc/index.js",
  "sideEffects": false,
  "license": "UNLICENSED",
  "author": "FiveQuarters.io",
  "scripts": {
    "build": "tsc -b",
    "test": "jest --colors",
    "coverage": "jest --colors --coverage",
    "lint": "tslint --fix -t stylish src/{,**/}*.ts{,x} test/{,**/}*.ts{,x}",
    "clean": "rm -r -f libc; rm -r -f libm; rm -r -f coverage",
    "nuke": "node libc/index.js"
  },
  "keywords": [],
  "dependencies": {
    "aws-sdk": "^2.1001.0"
  }
}
